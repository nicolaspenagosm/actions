name: Ping
env:
  REPO_OWNER: meroware
  REPO_NAME: example-event-dispatcher-workflows
on:
  repository_dispatch:
    types: [run-ping]
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: echo "Test"
